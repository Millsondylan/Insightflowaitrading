import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.insightflow.trading',
  appName: 'InsightFlow Trading',
  webDir: 'dist'
};

export default config;
